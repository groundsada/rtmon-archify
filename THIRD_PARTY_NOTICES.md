# Third-party notices

This repository distributes third-party code and assets. Their own licences
govern them; this file is where the picture is recorded.

## archify-cli/ (vendored upstream)

Upstream: <https://github.com/tt-a1i/archify>, pinned in
`archify-cli/VERSION` (2.17.0-dev.1).

- **Archify's own code: MIT** (`archify-cli/LICENSE`, Copyright (c) 2026
  tt-a1i (Archify), Copyright (c) 2025 Cocoon AI). Carries the usual
  permission to use, copy, modify, merge, publish, distribute and sublicense,
  on the condition that the notice travels with copies. The wrapper
  (`server.mjs`) appends that notice to every artifact it publishes, since a
  rendered diagram embeds Archify's viewer script.
- **JetBrains Mono subset: SIL OFL 1.1** (`archify-cli/assets/JetBrainsMono-OFL.txt`).
  The upstream template also carries the licence text inline in the font CSS,
  so each artifact is self-contained for this.

### The one file that is not upstream's

`archify-cli/renderers/shared/generated-brand-marks.mjs` is **deliberately
emptied** here and is not upstream's file. Upstream ships ~160 KB of
third-party brand logos there. Per upstream's own `THIRD_PARTY_NOTICES.md`
those include **CC-BY-NC-SA-4.0** (Vue.js - NonCommercial *and* ShareAlike),
CC-BY-SA-4.0 (Rust), CC-BY-SA-3.0 (Jenkins) and CC-BY-4.0 (Angular) artwork,
plus trademarked marks usable only under their brand guidelines. None of it
is reachable from the RTMon integration, which never sets a `brand` field on
a component, so keeping it would have pushed NonCommercial-licensed artwork
into this repository and every image for no functional gain. The file exports
an empty `BRAND_MARKS` array; `brand-marks.mjs` imports it, so it has to
exist.

**When re-vendoring, do not restore that file from upstream** unless the
individual marks have been cleared - the NonCommercial one especially. The
procedure in `README.md` re-empties it.

## Wrapper code

`server.mjs`, `test/` and the fixture IR are this repository's own code under
MIT (see `LICENSE`). The fixture IR was generated by ESnet's sense-rtmon
(`RTMonLibs/ArchifyIR.py`) from a real SENSE reservation model.
